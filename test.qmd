CONSIDER THE FOLLOWING WORKSHOP. 

I would like to create another similar workshop for my next set of lessons. 

My next set of lessons covers data viz taxonomies, multivariate visualization and for loops. The lessons for those are pasted below.

Help draft the workshop.


```{python}
import pandas as pd
import plotly.express as px
pd.set_option("mode.copy_on_write", True)



tips =  px.data.tips()
sunday = tips.query('day == "Sun"')
sunday['size_2'] = sunday['size'] + 2
sunday
```


```{python}
# mean tip column

tips['tip_mean'] = tips['tip'].mean()
tips

# mean
tips['bill_mean_per_day'] = tips.groupby('day')['total_bill'].transform('mean')
tips
```



```{python}
yao = pd.read_csv('/Users/kendavidn/Dropbox/Mac (2)/Downloads/yaounde_data 2.csv')

yao.

yao.query('~(is_smoker == "Smoker" | age > 50)')
```
